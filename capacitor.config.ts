import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nizam.angularpoc',
  appName: 'angular-poc',
  webDir: 'dist/angular-poc',
  bundledWebRuntime: false
};

export default config;
